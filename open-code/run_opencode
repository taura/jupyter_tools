#!/bin/bash
# OpenCode -> LiteLLM
# 設定は ~/.config/opencode/opencode.jsonc (このディレクトリからの symlink)
set -e
cd "$(dirname "$0")"
if [ ! -f client.env ]; then
    echo "client.env がありません。client.env.example をコピーして値を埋めてください。" >&2
    exit 1
fi
. ./client.env
exec opencode "$@"
